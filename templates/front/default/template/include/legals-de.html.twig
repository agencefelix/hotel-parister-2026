{% trans_default_domain 'front_default' %}

<h2>Definitionen</h2>

<p>
    <b>Kunde&nbsp;:</b> Jede professionelle, juristische oder natürliche Person, deren Rechtsfähigkeit gemäß Artikel 1123 und ff. des Bürgerlichen Gesetzbuches anerkannt wurde, die die Website unter Anwendung des vorliegenden rechtlichen Hinweises besucht.<br>
    <b>Angebote und Dienstleistungen&nbsp;:</b> <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> stellt dem Kunden zur Verfügung:
</p>
<p><b>Inhalt:</b> alle konstitutiven Elemente der auf der Website präsentierten Informationen, insbesondere Texte - Bilder -Videos.</p>
<p><b>Kundeninformationen:</b> Nachfolgend als "Informationen" bezeichnet und entsprechend allen persönlichen Daten, die von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> für die Verwaltung Ihres Kontos, die Verwaltung der Kundenbeziehung und für Analyse- und Statistikzwecke gespeichert werden können.</p>
<p><b>Benutzer:</b> Web-Benutzer, der die oben genannte Website besucht oder benutzt.</p>
<p><b>Personenbezogene Daten&nbsp;:</b> "Informationen, die in irgendeiner Form direkt oder indirekt die Identifizierung der natürlichen Personen ermöglichen, auf die sie sich beziehen" (Artikel 4 des Gesetzes Nr. 78-17 vom 6. Januar 1978).</p>
<p>Die Begriffe "personenbezogene Daten", "betroffene Person", "Auftragsverarbeiter" und "sensible Daten" haben die in der Allgemeinen Datenschutzverordnung (GDPR: Nr. 2016-679) definierte Bedeutung</p>
<h2 class="my-4 d-inline-block w-100">1. Präsentation der Website.</h2>
<p>Gemäß Artikel 6 des Gesetzes Nr. 2004-575 vom 21. Juni 2004 über das Vertrauen in die digitale Wirtschaft werden die Nutzer der Website  <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> über die Identität der verschiedenen Akteure informiert, die an der Umsetzung und Überwachung des Gesetzes beteiligt sind:</p>
<p>
    <strong>Eigentümer</strong>&nbsp;:
    {% if companyName %}{{ companyName|raw }}{% else %}<span class="text-danger bold">Raison sociale</span>{% endif %}
    {% if capital %} au capital de {{ capital|raw }}{% endif %}
    {% if vatNumber %} - Numéro de TVA: {{ vatNumber|raw }}{% endif %}
    {% if siretNumber %} - Numéro de SIRET: {{ siretNumber|raw }}{% endif %}
    {% if commercialRegisterNumber %} - Numéro registre du commerce&nbsp;: {{ commercialRegisterNumber|raw }}{% endif %}
    {% if companyAddress %} - {{ companyAddress|raw }}{% endif %}
    <br>
    <strong>Publikationsleiter</strong>&nbsp;:
    {% if managerName %}{{ managerName|raw }}{% else %}<span class="text-danger bold">Nom du responsable de la publication</span>{% endif %}
    {% if managerEmail %}{% if managerName %} - {% endif %}{{ managerEmail|raw }}{% else %}<span class="text-danger bold">Email du responsable de la publication</span>{% endif %}
    <br>
    Le responsable publication est une personne physique ou une personne morale.<br>
    <strong>Webmaster</strong>&nbsp;:
    {% if webmasterName %}{{ webmasterName|raw }}{% else %}<span class="text-danger bold">Nom du Webmaster</span>{% endif %}
    {% if webmasterEmail %}{% if webmasterName %} - {% endif %}{{ webmasterEmail|raw }}{% endif %}
    <br>
    <strong>Host-Anbieter</strong>&nbsp;:
    {% if hostName %}{{ hostName|raw }}{% else %}<span class="text-danger bold">Nom de l'hébergeur</span>{% endif %}<br>
    {% if hostAddress %}{{ hostAddress|raw }}{% else %}<span class="text-danger bold">Adresse hébergeur</span>{% endif %}
    <br>
    <strong>Datenschutzbeauftragter</strong>&nbsp;:
    {% if protectionOfficerName %}{{ protectionOfficerName|raw }}{% else %}<span class="text-danger bold">Nom du délégué à la protection des données</span>{% endif %}
    {% if protectionOfficerEmail %}{% if protectionOfficerName %} - {% endif %}{{ protectionOfficerEmail|raw }}{% else %}<span class="text-danger bold">Email du délégué</span>{% endif %}
    <br>
</p>
<h2 class="my-4 d-inline-block w-100">2. Allgemeine Bedingungen für die Nutzung der Website und der angebotenen Dienste.</h2>
<p>Diese Website ist ein geistiges Werk, das durch die Bestimmungen des Gesetzes über das geistige Eigentum und die geltenden internationalen Vorschriften geschützt ist. Der Kunde darf in keiner Weise alle oder einen Teil der Elemente oder Werke der Website wiederverwenden, abtreten oder in seinem eigenen Namen betreiben.</p>
<p>Die Nutzung von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> setzt die vollständige Annahme der folgenden Nutzungsbedingungen voraus. Diese Nutzungsbedingungen können jederzeit geändert oder ergänzt werden, so dass die Nutzer <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> aufgefordert sind, sie regelmäßig zu lesen.</p>
<p>Diese Website ist normalerweise jederzeit für die Nutzer zugänglich. Eine Unterbrechung aufgrund von technischen Wartungsarbeiten kann jedoch beschlossen werden von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> entschieden werden, die sich dann bemüht, die Nutzer im Voraus über die Daten und Uhrzeiten des Eingriffs zu informieren. Die Website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wird regelmäßig aktualisiert von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verantwortlich. Ebenso können die rechtlichen Hinweise jederzeit geändert werden: sie sind dennoch für den Nutzer verbindlich, der aufgefordert wird, sie so oft wie möglich zu lesen.</p>
<h2 class="my-4 d-inline-block w-100">3. Beschreibung der erbrachten Dienstleistungen.</h2>
<p>Die Website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> soll über alle Aktivitäten des Unternehmens informieren. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> ist bestrebt, möglichst genaue Informationen über <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zu liefern. Sie kann jedoch nicht für eventuelle Auslassungen, Ungenauigkeiten oder Unzulänglichkeiten bei der Aktualisierung verantwortlich gemacht werden, sei es auf eigene Rechnung oder auf Rechnung von Drittpartnern, die sie mit diesen Informationen versorgen.</p>
<p>Alle auf <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> bereitgestellten Informationen sind Richtwerte und können sich ändern. Die Informationen auf <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> erheben keinen Anspruch auf Vollständigkeit. Alle Informationen können seit ihrer Veröffentlichung im Internet geändert werden.</p>
<h2 class="my-4 d-inline-block w-100">4. Vertragliche Beschränkungen der technischen Daten.</h2>
<p>Die Website verwendet die JavaScript-Technologie. Sie kann nicht für materielle Schäden im Zusammenhang mit der Nutzung der Website verantwortlich gemacht werden. Darüber hinaus akzeptiert der Nutzer der Website, auf die Website mit einem aktuellen, virenfreien Material und mit einem aktualisierten Browser der neuesten Generation zuzugreifen. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wird von einem Dienstleister im Gebiet der Europäischen Union in Übereinstimmung mit den Bestimmungen der Allgemeinen Datenschutzverordnung (GDPR: Nr. 2016-679) gehostet.</p>
<p>Ziel ist es, eine Leistung zu erbringen, die die bestmögliche Erreichbarkeit gewährleistet. Der Host gewährleistet die Kontinuität seines Dienstes 24 Stunden am Tag, an jedem Tag des Jahres. Er behält sich jedoch das Recht vor, den Hosting-Service für möglichst kurze Zeiträume zu unterbrechen, insbesondere zum Zweck der Wartung und Verbesserung seiner Infrastruktur, bei einem Ausfall seiner Infrastruktur oder wenn die Dienste und Services einen als anormal erachteten Datenverkehr erzeugen.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> und der Gastgeber haften nicht für Störungen des Internets, der Telefonleitungen oder der zugehörigen Computer- und Telefonieausrüstung.</p>
<h2 class="my-4 d-inline-block w-100">5. Geistiges Eigentum und Fälschungen.</h2>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> ist Inhaber der Rechte am geistigen Eigentum und besitzt die Nutzungsrechte für alle auf der Website zugänglichen Elemente, einschließlich Texten, Bildern, Grafiken, Logos, Videos, Icons und Tönen. Jegliche Vervielfältigung, Darstellung, Änderung, Veröffentlichung, Anpassung aller oder eines Teils der Elemente der Website, unabhängig von den verwendeten Mitteln oder Verfahren, ist verboten, es sei denn, es liegt eine vorherige schriftliche Genehmigung vor: <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a></p>
<p>Jede unbefugte Nutzung der Website oder eines der darin enthaltenen Elemente wird als Verstoß betrachtet und gemäß den Bestimmungen der Artikel L.335-2 ff. des Code de Propriété Intellectuelle verfolgt.</p>
<h2 class="my-4 d-inline-block w-100">6. Beschränkung der Haftung.</h2>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> fungiert als Herausgeber der Website. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> ist für die Qualität und den Wahrheitsgehalt der von ihr veröffentlichten Inhalte verantwortlich.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kann nicht für direkte oder indirekte Schäden am Material des Nutzers verantwortlich gemacht werden, die beim Zugriff auf <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> entstehen und entweder auf die Verwendung von Geräten zurückzuführen sind, die nicht den unter Punkt 4 genannten Spezifikationen entsprechen, oder auf das Auftreten eines Fehlers oder einer Inkompatibilität.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kann auch nicht für indirekte Schäden (wie z.B. Markt- oder Gelegenheitsverluste) haftbar gemacht werden, die sich aus der Nutzung von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> ergeben. Den Nutzern stehen interaktive Bereiche (Möglichkeit, im Kontaktbereich Fragen zu stellen) zur Verfügung. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> behält sich das Recht vor, ohne Vorankündigung alle in diesem Bereich hinterlegten Inhalte zu löschen, die gegen die in Frankreich geltende Gesetzgebung, insbesondere gegen die Datenschutzbestimmungen, verstoßen würden. Gegebenenfalls behält sich <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> auch das Recht vor, die zivil- und/oder strafrechtliche Haftung des Nutzers anzufechten.</p>
<h2 class="my-4 d-inline-block w-100">7. Verwaltung der personenbezogenen Daten.</h2>
<p>Der Kunde wird über die Vorschriften zur Marketingkommunikation, das Gesetz vom 21. Juni 2014 über das Vertrauen in die digitale Wirtschaft, das Datenschutzgesetz vom 06. August 2004 und die allgemeine Datenschutzverordnung (GDPR: Nr. 2016-679) informiert.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.1 Für die Erhebung personenbezogener Daten verantwortliche Personen</h3>
<p>Für die persönlichen Daten, die im Zusammenhang mit der Erstellung des persönlichen Kontos des Nutzers und seiner Navigation auf der Website gesammelt werden, ist die Person, die für die Verarbeitung der persönlichen Daten verantwortlich ist: FIRMA. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wird vertreten durch {% if companyRepresentativeName %}{{ companyRepresentativeName|raw }}{% else %}<span class="text-danger bold">Nom du représentant légal de l’entreprise</span>{% endif %}, seinen gesetzlichen Vertreter</p>
<p>Als Verantwortlicher für die von ihm gesammelten Daten verpflichtet sich <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zur Einhaltung.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.2 Zweck der erhobenen Daten</h3>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kann einige oder alle Daten verarbeiten:</p>
<ul>
    <li>um die Navigation auf der Website und die Verwaltung und Rückverfolgbarkeit der vom Nutzer bestellten Dienste und Leistungen zu ermöglichen: Anmelde- und Nutzungsdaten der Website, Rechnungsstellung, Bestellhistorie usw.</li>
    <li>zur Vorbeugung und Bekämpfung von Computerbetrug (Spamming, Hacking usw.): für das Surfen verwendete Computerausrüstung, IP-Adresse, Passwort (gehasht)</li>
    <li>zur Verbesserung der Navigation auf der Website: Anmelde- und Nutzungsdaten</li>
    <li>zur Durchführung von fakultativen Zufriedenheitsumfragen auf <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>&nbsp;: E-Mail Adresse</li>
    <li>zur Durchführung von Kommunikationskampagnen (SMS, Post): Telefonnummer, E-Mail Adresse</li>
</ul>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> vermarktet Ihre personenbezogenen Daten nicht und verwendet sie daher nur im Bedarfsfall oder für statistische und analytische Zwecke.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.3 Recht auf Auskunft, Berichtigung und Widerspruch</h3>
<p>Gemäß den geltenden europäischen Vorschriften haben die Nutzer von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> die folgenden Rechte:</p>
<ul>
    <li>Recht auf Auskunft (Artikel 15 DSGVO) und Berichtigung (Artikel 16 DSGVO), Aktualisierung, Vollständigkeit der Nutzerdaten, Recht auf Sperrung oder Löschung personenbezogener Nutzerdaten (Artikel 17 DSGVO), wenn diese unrichtig, unvollständig, zweideutig oder veraltet sind oder deren Erhebung, Verwendung, Weitergabe oder Speicherung verboten ist</li>
    <li>Recht auf jederzeitigen Widerruf der Einwilligung (Artikel 13-2c DSGVO)</li>
    <li>Recht auf Einschränkung der Verarbeitung von Nutzerdaten (Artikel 18 GDPR)</li>
    <li>das Recht, den Verbleib der Daten des Nutzers nach dessen Tod zu bestimmen und zu wählen, an wen <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> seine Daten weitergeben soll (oder nicht). Sobald <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> vom Tod eines Nutzers erfährt und in Ermangelung von Anweisungen seinerseits, verpflichtet sich <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, seine Daten zu vernichten, es sei denn, ihre Aufbewahrung ist zu Beweiszwecken oder zur Erfüllung einer gesetzlichen Verpflichtung erforderlich.</li>
</ul>
<p>Wenn der Nutzer wissen möchte, wie <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> seine personenbezogenen Daten verwendet, um deren Berichtigung ersucht oder sich ihrer Verarbeitung widersetzt, kann er sich schriftlich an die folgende Adresse wenden: {% if managerEmail %}{{ managerEmail }}{% else %} <span class="text-danger bold">Manager email</span>{% endif %}</p>
<p>COMPANY - DPO,
    {% if protectionOfficerName %}{{ protectionOfficerName|raw }}{% else %}<span class="text-danger bold">Nom du délégué à la protection des données</span>{% endif %}
    {% if protectionOfficerAddress %}{% if protectionOfficerName %} <br> {% endif %}{{ protectionOfficerAddress|raw }}{% endif %}
</p>
<p>In diesem Fall muss der Nutzer die personenbezogenen Daten angeben, die er <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> korrigieren, aktualisieren oder löschen lassen möchte, wobei er sich durch eine Kopie eines Ausweises (Personalausweis oder Reisepass) genau identifizieren muss.</p>
<p>Anträge auf Löschung personenbezogener Daten unterliegen den Verpflichtungen, die für <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> auferlegt werden, insbesondere was die Aufbewahrung oder Archivierung von Dokumenten betrifft. Zum Schluss, <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> eine Beschwerde bei den Aufsichtsbehörden einreichen, insbesondere bei der CNIL (<a
        href="https://www.cnil.fr/en/plaintes" aria-label="CNIL">https://www.cnil.fr/en/plaintes</a>).</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.4 Nichtmitteilung von personenbezogenen Daten</h3>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verarbeitet, hostet oder überträgt die über seine Kunden gesammelten Informationen nicht in ein Land außerhalb der Europäischen Union oder in ein Land, das von der Europäischen Kommission als "unzureichend" eingestuft wurde, ohne den Kunden vorher darüber zu informieren. Allerdings, <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> bleibt jedoch frei in der Wahl seiner technischen und kommerziellen Unterauftragnehmer, sofern diese ausreichende Garantien hinsichtlich der Anforderungen der Allgemeinen Datenschutzverordnung (GDPR: Nr. 2016-679) bieten.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verpflichtet sich, alle notwendigen Vorkehrungen zu treffen, um die Sicherheit der Informationen zu wahren und insbesondere sicherzustellen, dass sie nicht an Unbefugte weitergegeben werden. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wird jedoch, wenn ein Vorfall, der die Integrität oder Vertraulichkeit der Informationen des Kunden beeinträchtigt, zur Kenntnis gebracht wird, den Kunden unverzüglich informieren und ihn über die getroffenen Abhilfemaßnahmen unterrichten. Darüber hinaus sammelt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> keine "sensiblen Daten".</p>
<p>Die personenbezogenen Daten des Nutzers können von Tochtergesellschaften der <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> und Unterauftragnehmern (Dienstleistern) verarbeitet werden, und zwar ausschließlich, um die Ziele dieser Politik zu erreichen.</p>
<p>Im Rahmen ihrer jeweiligen Zuständigkeiten und zu den oben genannten Zwecken sind die wichtigsten Personen, die Zugang zu den Daten der Nutzer von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> haben können, hauptsächlich die Mitarbeiter unseres Kundendienstes.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.5 Arten der erhobenen Daten</h3>
<p>Von den Nutzern der Website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> erheben wir die folgenden Daten, die für den Betrieb des Dienstes unerlässlich sind und die für einen Zeitraum von maximal 9 Monaten nach Beendigung des Vertragsverhältnisses aufbewahrt werden.
    Welche Daten werden erhoben</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> sammelt auch Daten, um die Nutzererfahrung zu verbessern und kontextbezogene Ratschläge zu geben: Daten zur Verbesserung der Nutzererfahrung und zur Bereitstellung kontextbezogener Ratschläge</p>
<p>Diese Daten werden für einen Zeitraum von maximal 9 Monaten nach Beendigung des Vertragsverhältnisses aufbewahrt.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">8. Benachrichtigung über einen Vorfall</h3>
<p>Trotz aller Bemühungen ist keine Methode der Übertragung über das Internet und keine Methode der elektronischen Speicherung vollkommen sicher. Wir können daher keine absolute Sicherheit garantieren. Sollten wir von einer Sicherheitsverletzung Kenntnis erlangen, würden wir die betroffenen Nutzer benachrichtigen, damit sie geeignete Maßnahmen ergreifen können. Unsere Verfahren zur Meldung von Vorfällen berücksichtigen unsere rechtlichen Verpflichtungen auf nationaler oder europäischer Ebene. Wir verpflichten uns, unsere Kunden umfassend über alle Angelegenheiten zu informieren, die die Sicherheit ihres Kontos betreffen, und ihnen alle erforderlichen Informationen zur Verfügung zu stellen, damit sie ihren eigenen gesetzlichen Meldepflichten nachkommen können.</p>
<p>Keine persönlichen Informationen des Nutzers der Website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>werden ohne das Wissen des Nutzers veröffentlicht, ausgetauscht, übertragen, abgetreten oder auf irgendeinem Medium an Dritte verkauft. Nur die Hypothese des Erwerbs von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> und seiner Rechte würde die Übermittlung der genannten Informationen an den potenziellen Käufer erlauben, der seinerseits an die gleiche Verpflichtung zur Aufbewahrung und Änderung der Daten gegenüber dem Nutzer der Website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> gebunden wäre.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">Sicherheit</h3>
<p>Um die Sicherheit und Vertraulichkeit der personenbezogenen Daten und der persönlichen Gesundheitsdaten zu gewährleisten, verwendet <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> Netzwerke, die durch Standardeinrichtungen wie Firewalls, Pseudonymisierung, Verschlüsselung und Passwörter geschützt sind.</p>
<p>Bei der Verarbeitung personenbezogener Daten, <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> alle angemessenen Maßnahmen ergreifen, um sie vor Verlust, Missbrauch, unbefugtem Zugriff, Offenlegung, Änderung oder Zerstörung zu schützen.</p>
<h2 class="my-4 d-inline-block w-100">9. Hypertext und Cookies</h2>
<h3 class="mt-1 mb-3 d-inline-block w-100">9.1. « COOKIES »</h3>
<p>Ein "Cookie" ist eine kleine Informationsdatei, die an den Browser des Nutzers gesendet und auf dem Endgerät des Nutzers (z. B. Computer, Smartphone) gespeichert wird (im Folgenden "Cookies"). Diese Datei enthält Informationen wie den Domänennamen des Nutzers, den Internetdienstanbieter des Nutzers, das Betriebssystem des Nutzers sowie das Datum und die Uhrzeit des Zugriffs. Unter keinen Umständen dürfen Cookies das Endgerät des Nutzers beschädigen.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kann die Informationen des Nutzers über seinen Besuch auf der Website, wie z.B. die besuchten Seiten und die durchgeführten Suchen, verarbeiten. Diese Informationen ermöglichen es <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, den Inhalt der Website und die Navigation des Nutzers zu verbessern.</p>
<p>Da Cookies die Navigation und/oder die Bereitstellung der von der Website angebotenen Dienste erleichtern, kann der Nutzer seinen Browser so konfigurieren, dass er entscheiden kann, ob er sie akzeptiert oder nicht, so dass die Cookies im Endgerät gespeichert werden, oder ob sie im Gegenteil abgelehnt werden sollen, entweder systematisch oder je nach ihrem Aussteller. Der Nutzer kann seine Browsersoftware auch so konfigurieren, dass ihm die Annahme oder Ablehnung von Cookies von Zeit zu Zeit angeboten wird, bevor ein Cookie in seinem Endgerät gespeichert wird. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> informiert den Nutzer, dass in diesem Fall möglicherweise nicht alle Funktionen seiner Navigationssoftware zur Verfügung stehen.</p>
<p>Dies kann auch der Fall sein, wenn <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> oder einer seiner Dienstleister aus Gründen der technischen Kompatibilität den vom Endgerät verwendeten Browsertyp, die Sprach- und Anzeigeeinstellungen oder das Land, aus dem das Endgerät mit dem Internet verbunden zu sein scheint, nicht erkennen kann.</p>
<p>Gegebenenfalls lehnt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> jede Verantwortung für die Folgen ab, die sich aus der Beeinträchtigung des Funktionierens der Website und der von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> angebotenen Dienste ergeben, wenn (i) der Nutzer die Cookies ablehnt oder (ii) <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> aufgrund der Wahl des Nutzers nicht in der Lage ist, die für den Betrieb der Website erforderlichen Cookies zu speichern oder abzurufen. Für die Verwaltung von Cookies und die Wahl des Benutzers ist die Konfiguration jedes Browsers unterschiedlich. Sie wird im Hilfemenü des Browsers beschrieben, wo Sie erfahren können, wie der Benutzer seine Wünsche bezüglich Cookies ändern kann.
    Der Nutzer hat jederzeit die Möglichkeit, seine Wünsche in Bezug auf Cookies zu äußern und zu ändern. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kann auch die Dienste von externen Dienstleistern in Anspruch nehmen, um die in diesem Abschnitt beschriebenen Informationen zu sammeln und zu verarbeiten.</p>
<p>Schließlich können Twitter, Facebook, Linkedin und Google Plus durch Anklicken der Symbole für die sozialen Netzwerke Twitter, Facebook, Linkedin und Google Plus auf der Website von https://alpex-cartier.felix-dev.com oder ihrer mobilen Anwendung und wenn der Nutzer die Hinterlegung von Cookies akzeptiert hat, indem er auf der Website oder der mobilen Anwendung von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> weitersurft, auch Cookies auf Ihren Geräten (Computer, Tablet, Mobiltelefon) ablegen.</p>
<p>Diese Arten von Cookies werden nur mit Ihrer Zustimmung auf Ihren Geräten gespeichert, indem Sie die Website oder die mobile Anwendung von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> weiter nutzen. Der Nutzer kann jedoch jederzeit seine Zustimmung zur Speicherung dieser Art von Cookies auf <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> widerrufen.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">Artikel 9.2. INTERNET-TAGS</h3>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kann gelegentlich Internet-Tags (auch "Tags" oder Aktions-Tags, Ein-Pixel-GIFs, transparente GIFs, unsichtbare GIFs und Eins-zu-Eins-GIFs genannt) verwenden und diese über einen Webanalysepartner einsetzen, der sich möglicherweise in einem anderen Land befindet (und somit die entsprechenden Informationen, einschließlich der IP-Adresse des Nutzers, speichert).</p>
<p>Diese Tags werden sowohl in der Online-Werbung, die den Benutzern den Zugang zur Website ermöglicht, als auch auf den verschiedenen Seiten der Website platziert.
    Diese Technologie ermöglicht es <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, die Reaktionen der Besucher auf die Website und die Effektivität ihrer Aktionen (z. B. die Anzahl der Seitenaufrufe und der abgerufenen Informationen) sowie die Nutzung dieser Website durch den Nutzer zu bewerten.</p>
<p>Der externe Dienstleister kann über diese Tags Informationen über die Besucher der Website und anderer Internetseiten sammeln, Berichte über die Aktivitäten auf der Website zuhanden von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> erstellen und andere Dienstleistungen im Zusammenhang mit der Nutzung der Website und des Internets erbringen.</p>
<h2 class="my-4 d-inline-block w-100">10. Anwendbares Recht und Gerichtsbarkeit.</h2>
<p>Jede Streitigkeit im Zusammenhang mit der Nutzung von <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> unterliegt dem französischen Recht. Abgesehen von den Fällen, in denen das Gesetz dies nicht zulässt, wird die ausschließliche Zuständigkeit im Falle eines Rechtsstreits den zuständigen Gerichten der Stadt des Gerichtsstandes übertragen.</p>