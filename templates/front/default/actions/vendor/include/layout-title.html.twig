{% trans_default_domain 'front_default' %}

{#{% if title|striptags|length > 0 %}#}
{#    <h{{ titleForce }} class="title mt-4 mb-4">{{ intl.title|raw }}</h{{ titleForce }}>#}
{#    {% if entity.category and entity.category.displayCategory and intlCategory.title is defined and intlCategory.title %}#}
{#        <h{{ titleForce + 1 }} class="{{ interfaceName }}-category-title">{{ intlCategory.title|unescape|raw }}</h{{ titleForce + 1 }}>#}
{#    {% endif %}#}
{#{% endif %}#}