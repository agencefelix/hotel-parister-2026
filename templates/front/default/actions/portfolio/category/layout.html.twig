{% trans_default_domain 'front_default' %}

{% set blockType = block.blockType is defined and block.blockType ? block.blockType.slug : null %}
{% set intl = intl is defined ? intl : null %}
{% set isInBox = true %}

{% if blockType and intl %}

{% endif %}